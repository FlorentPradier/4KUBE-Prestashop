apiVersion: v1
kind: Service
metadata:
  name: {{ include "4KUBE-Prestashop.fullname" . }}
  labels:
    {{- include "4KUBE-Prestashop.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    {{- include "4KUBE-Prestashop.selectorLabels" . | nindent 4 }}
