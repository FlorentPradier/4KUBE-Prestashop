apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "4KUBE-Prestashop.fullname" . }}-test-connection"
  labels:
    {{- include "4KUBE-Prestashop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "4KUBE-Prestashop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
